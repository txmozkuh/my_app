import { Container } from '@/components/ui/container'

export default function LayoutHeader() {
  return (
    <header id='site-header' className='fixed text-a-white top-0 left-0 z-50'>
      <Container>Header</Container>
    </header>
  )
}
